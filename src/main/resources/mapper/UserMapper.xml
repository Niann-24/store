<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cy.store.mapper.UserMapper">

<!--    useGeneratedKeys    开启某字段自增   keyProperty  设置哪个字段为主键自增   -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="uid">
        INSERT INTO t_user(username, password, salt, phone, email, gender, avatar, is_delete, created_user,
                           created_time, modified_user, modified_time)
        VALUES (
                #{username}, #{password}, #{salt}, #{ phone}, #{email}, #{gender}, #{avatar}, #{isDelete},
                #{createdUser},
                #{ createdTime}, #{modifiedUser}, #{modifiedTime}
                )
    </insert>




<!--
id: 表示resultMap的值
 type：  取值是一个类，表示数据库查询结果与java实体类进行结果集映射-->
    <resultMap id="UserEntityMap" type="com.cy.store.entity.User">
<!--        将不一致的字段进行指定，一致的可以不写-->
<!--        ！！！定义映射规则时主键不可省略！！！-->
        <id column="uid" property="uid"/>
        <result column="is_delete" property="isDelete"/>
        <result column="created_user" property="createdUser"/>
        <result column="created_time" property="createdTime"/>
        <result column="modified_user" property="modifiedUser"/>
        <result column="modified_time" property="modifiedTime"/>
    </resultMap>

<!--    select查询的结果是一个对象或多个对象
      表的字段和类的对象字段名称一致时使用：  resultType：表示查询的结果集，指定对应映射的类型，并且包含完整包结构
       表的字段和类的对象字段名称不一致时使用： resultMap：表的字段和类的对象字段名称不一致时自定义查询 结果集的映射规则
-->
    <select id="findByUsername" resultMap="UserEntityMap">
        SELECT * from t_user WHERE username=#{usernmae}
    </select>


</mapper>